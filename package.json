{
  "name": "scraper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "devDependencies": {
    "esbuild": "^0.18.6",
    "esbuild-node-externals": "^1.7.0",
    "ts-node": "^10.9.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsc src/index.ts --watch --module es2022 --outfile build/index.js && node build/index.js",
    "compile": "tsc src/index.ts --module es2022 --moduleResolution bundler --target es2022 --outDir compiled",
    "build": "npm run compile && node build.js && rm -r compiled",
    "start": "npm run build && node build/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "node-fetch": "^3.3.1",
    "node-html-parser": "^6.1.5",
    "puppeteer": "^20.7.3",
    "typescript": "^5.1.3"
  }
}
